import React, { Component } from "react";
import "./BadViewport.scss";

class BadViewport extends Component {
  render() {
    return(
      <div id="bad-viewport-container">bad viewport</div>
    )
  }
}

export default BadViewport;
